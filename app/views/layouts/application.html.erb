<!DOCTYPE html>
<html> 
  <head>
    <title>CU | LE | KE</title> 
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%=render 'layouts/rails_default'%>
    <%=render 'layouts/shim'%>
  </head> 
  <body>
    <%=render 'layouts/header'%>
    <!--<div class= "jumbotron jumbotron-fluid">
      <div class= "container">
        <%= image_tag  "banner_culeke", alt: "banner-culeke w-100", class: "img-fluid"%>
        <div class="centered">CU LE KE</div>
      </div>
    <div>-->
    <div class="container">
      <%= yield %> 
      <%=render 'layouts/footer'%>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body> 
</html>
 